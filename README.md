## Relay-modern

In the project directory, you can run:

### `yarn update-schema`

Update schema based on api endpoint at `src/api.js`

### `yarn relay`

Execute relay compiler to update the relay-based components. Also possible run with `--watch` to detect all changes automatically.

### `yarn start`

Runs the application.
